import { FilterActividadPipe } from './filter-actividad.pipe';

describe('FilterActividadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActividadPipe();
    expect(pipe).toBeTruthy();
  });
});
