import { FilterDudaPipe } from './filter-duda.pipe';

describe('FilterDudaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDudaPipe();
    expect(pipe).toBeTruthy();
  });
});
