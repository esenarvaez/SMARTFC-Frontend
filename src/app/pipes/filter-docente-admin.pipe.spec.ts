import { FilterDocenteAdminPipe } from './filter-docente-admin.pipe';

describe('FilterDocenteAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDocenteAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
