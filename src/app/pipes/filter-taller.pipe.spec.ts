import { FilterTallerPipe } from './filter-taller.pipe';

describe('FilterTallerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTallerPipe();
    expect(pipe).toBeTruthy();
  });
});
