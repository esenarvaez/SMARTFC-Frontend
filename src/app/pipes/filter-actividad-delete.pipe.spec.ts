import { FilterActividadDeletePipe } from './filter-actividad-delete.pipe';

describe('FilterActividadDeletePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActividadDeletePipe();
    expect(pipe).toBeTruthy();
  });
});
