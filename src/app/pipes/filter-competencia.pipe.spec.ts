import { FilterCompetenciaPipe } from './filter-competencia.pipe';

describe('FilterCompetenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompetenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
