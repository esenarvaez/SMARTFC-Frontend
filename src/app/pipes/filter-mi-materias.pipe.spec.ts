import { FilterMiMateriasPipe } from './filter-mi-materias.pipe';

describe('FilterMiMateriasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMiMateriasPipe();
    expect(pipe).toBeTruthy();
  });
});
