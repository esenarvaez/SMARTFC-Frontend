import { FilterContentDeletePipe } from './filter-content-delete.pipe';

describe('FilterContentDeletePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterContentDeletePipe();
    expect(pipe).toBeTruthy();
  });
});
