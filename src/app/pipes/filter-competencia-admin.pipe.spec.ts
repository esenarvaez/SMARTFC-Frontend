import { FilterCompetenciaAdminPipe } from './filter-competencia-admin.pipe';

describe('FilterCompetenciaAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompetenciaAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
